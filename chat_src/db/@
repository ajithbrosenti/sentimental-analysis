<?php
    $hostname = "localhost";
    $username = "moodle";
    $pwd = "Moodle@123";
    $db = "chat";

    $conn = mysqli_connect($hostname, $username, $pwd, $db);
    
    if( !$conn ) 
        die("CONNECTION FAILED: ".mysqli_connect_error());
?>
